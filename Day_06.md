
# Django MVT Architecture

MVT stands for **Model-View-Template**. It is the design pattern used by Django to structure web applications.

Unlike the classic **MVC (Model-View-Controller)** pattern, Django handles the Controller part itself via the framework. The developer focuses on Model, View, and Template.

# today we explore T - template